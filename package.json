{
  "name": "ivan_minsk_apartment_bot",
  "version": "0.1.5",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "start": "yarn build && node build.js",
    "build": "esbuild index.ts --bundle --outfile=build.js --target=node14.15 --platform=node"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "proposal-temporal": "^0.8.0",
    "yaml": "2.0.0-6"
  },
  "devDependencies": {
    "@types/node": "14.x",
    "esbuild": "^0.12.1",
    "prettier": "^2.3.0",
    "typescript": "^4.2.4"
  }
}
